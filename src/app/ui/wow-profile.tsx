
// TODO: Only optionally null to satisfy type for useSearchParam(). Consider being more strict in the future.
export default function WowProfile({ code }: { code: string | null }) {



  return (
    <p>Wow Profile Component</p>
  )
}